import React from 'react'
import version from '../images/version.png'
const Version = () => {
    return (
      <section className='version'>
        <h1> Verison beta 1.00. Currently Working On More Relational Updates.</h1>
        <img src={version}/>
      </section>
    );
}

export default Version
